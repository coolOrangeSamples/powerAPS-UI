<wpf:ThemedWPFWindow xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wpf="clr-namespace:Autodesk.DataManagement.Client.Framework.Forms.Controls.WPF;assembly=Autodesk.DataManagement.Client.Framework.Forms"
    Title="Select File/Folder" 
    Height="450" Width="400" MinWidth="450" MinHeight="300" FontFamily="Segoe UI" FontSize="12.5"
    Icon="pack://application:,,,/powerAPS.Utils;component/Resources/coolorange.ico"
    WindowStartupLocation="CenterOwner">

    <wpf:ThemedWPFWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Autodesk.DataManagement.Client.Framework.Forms;component/Controls/WPF/ControlTemplates/MergedResources.xaml" />
                <ResourceDictionary Source="pack://application:,,,/powerAPS.Utils;component/Controls/WPF/ImageResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ThemedButtonStyle}">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Height" Value="29" />
            </Style>
            <Style TargetType="{x:Type Label}" BasedOn="{StaticResource ThemedLabelStyle}">
                <Setter Property="FontSize" Value="12" />
            </Style>
            <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource ThemedTreeViewItemStyle}">
                <Setter Property="FontSize" Value="12" />
            </Style>
            <Style TargetType="{x:Type TreeView}" BasedOn="{StaticResource ThemedTreeViewStyle}">
                <Setter Property="FontSize" Value="12" />
            </Style>
        </ResourceDictionary>
    </wpf:ThemedWPFWindow.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label x:Name="Label" Grid.Row="0" >
            <TextBlock TextWrapping="WrapWithOverflow">
                Select File/Folder
            </TextBlock>
        </Label>

        <TreeView x:Name="TreeView" Grid.Row="1" ItemsSource="{Binding}" Margin="0,0,0,25" >
            <TreeView.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Foreground" Value="{DynamicResource TreeView.Item.Foreground}" />
                </Style>
            </TreeView.Resources>
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource ThemedTreeViewItemStyle}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="Foreground" Value="{DynamicResource TreeView.Item.Foreground}" />
                    <Style.Triggers>
                        <!--<Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="#adc6e5"/>
                        </Trigger>-->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                    </Style.Triggers>
                    <Style.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="0"/>
                        </Style>
                    </Style.Resources>
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="17" Height="16" Margin="3,1" >
                            <Image.Style>
                                <Style TargetType="{x:Type Image}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Type}" Value="File">
                                            <Setter Property="Source" Value="{StaticResource FileImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Folder">
                                            <Setter Property="Source" Value="{StaticResource FolderImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Fusion">
                                            <Setter Property="Source" Value="{StaticResource FusionLogo}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="ACC">
                                            <Setter Property="Source" Value="{StaticResource AccProjectImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="BIM360">
                                            <Setter Property="Source" Value="{StaticResource Bim360ProjectImage}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                        <TextBlock Text="{Binding Name}" Margin="3,0,5,0" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="Ok" Grid.Column="1" Width="100" Margin="0,0,10,0">OK</Button>
            <Button x:Name="Cancel" Grid.Column="2" Width="100" IsCancel="True">Cancel</Button>
        </Grid>
    </Grid>
</wpf:ThemedWPFWindow>