<wpf:ThemedWPFWindow xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wpf="clr-namespace:Autodesk.DataManagement.Client.Framework.Forms.Controls.WPF;assembly=Autodesk.DataManagement.Client.Framework.Forms"
    Title="Vault Folder Settings" 
    Height="360" Width="400" MinWidth="400" MinHeight="360" FontFamily="Segoe UI" FontSize="12.5"
    Icon="pack://application:,,,/powerAPS.Utils;component/Resources/coolorange.ico"
    WindowStartupLocation="CenterOwner">

    <wpf:ThemedWPFWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Autodesk.DataManagement.Client.Framework.Forms;component/Controls/WPF/ControlTemplates/MergedResources.xaml" />
                <ResourceDictionary Source="pack://application:,,,/powerAPS.Utils;component/Controls/WPF/ImageResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ThemedComboBoxStyle}">
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Margin" Value="1" />
                <Setter Property="Height" Value="28" />
            </Style>
            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ThemedButtonStyle}">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Height" Value="29" />
            </Style>
            <Style TargetType="{x:Type Label}" BasedOn="{StaticResource ThemedLabelStyle}">
                <Setter Property="FontSize" Value="12" />
            </Style>
        </ResourceDictionary>
    </wpf:ThemedWPFWindow.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <GroupBox Header="Vault Folder Settings" Grid.Row="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>

                <Label Content="Folder Category to identify ACC Projects" Grid.Row="0" />
                <ComboBox 
                    SelectedValue="{Binding Path=DataContext.Category, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    Grid.Row="1" 
                    ItemsSource="{Binding Path=DataContext.Categories, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    SelectedValuePath="Name"
                    DisplayMemberPath="Name" />

                <Label Content="Folder Property to persist an ACC Account (Hub)" Grid.Row="2" />
                <ComboBox 
                    SelectedValue="{Binding Path=DataContext.Hub, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    Grid.Row="3" 
                    ItemsSource="{Binding Path=DataContext.Properties, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    SelectedValuePath="DispName" 
                    DisplayMemberPath="DispName" />

                <Label Content="Folder Property to persist an ACC Project" Grid.Row="4" />
                <ComboBox 
                    SelectedValue="{Binding Path=DataContext.Project, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    Grid.Row="5" 
                    ItemsSource="{Binding Path=DataContext.Properties, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    SelectedValuePath="DispName" 
                    DisplayMemberPath="DispName" />

                <Label Content="Folder Property to persist an ACC Folder" Grid.Row="6" />
                <ComboBox 
                    SelectedValue="{Binding Path=DataContext.Folder, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    Grid.Row="7" 
                    ItemsSource="{Binding Path=DataContext.Properties, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                    SelectedValuePath="DispName" 
                    DisplayMemberPath="DispName" />
            </Grid>
        </GroupBox>

        <Grid Grid.Row="2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="Ok" Grid.Column="1" Width="100" Margin="0,0,10,0">OK</Button>
            <Button x:Name="Cancel" Grid.Column="2" Width="100" IsCancel="True">Cancel</Button>
        </Grid>
    </Grid>
    </wpf:ThemedWPFWindow>